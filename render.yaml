services:
  # Core API Web Service
  - type: web
    name: servio-backend
    env: node
    region: oregon
    plan: starter
    buildCommand: cd backend && npm run install:ci && npm run build
    startCommand: cd backend && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3002
      - key: DATABASE_URL
        fromDatabase:
          name: servio-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        fromService:
          type: web
          name: servio-web
          property: host
      - key: OPENAI_API_KEY
        sync: false
      - key: STORAGE_BUCKET
        sync: false
      - key: STORAGE_REGION
        value: us-east-1
      - key: STORAGE_ACCESS_KEY
        sync: false
      - key: STORAGE_SECRET_KEY
        sync: false
      - key: STORAGE_ENDPOINT
        sync: false
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_PHONE_NUMBER
        sync: false
      - key: SESV2_REGION
        value: us-east-1
      - key: SESV2_ACCESS_KEY_ID
        sync: false
      - key: SESV2_SECRET_ACCESS_KEY
        sync: false

  # Background Worker
  - type: worker
    name: servio-worker
    env: node
    region: oregon
    plan: starter
    buildCommand: cd backend && npm ci --legacy-peer-deps && npm run build
    startCommand: cd backend && npm run start:worker
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: servio-db
          property: connectionString
      - key: WORKER_POLL_INTERVAL
        value: "5000"
      - key: STORAGE_BUCKET
        sync: false
      - key: STORAGE_ACCESS_KEY
        sync: false
      - key: STORAGE_SECRET_KEY
        sync: false

  # Frontend Web Service
  - type: web
    name: servio-web
    env: node
    region: oregon
    plan: starter
    buildCommand: cd frontend && npm run install:ci && npm run build
    startCommand: cd frontend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_OPTIONS
        value: "--max-old-space-size=2048"
      - key: NEXT_PUBLIC_API_URL
        fromService:
          type: web
          name: servio-backend
          property: host
      - key: NEXT_PUBLIC_WS_URL
        fromService:
          type: web
          name: servio-backend
          property: host
      - key: NEXT_PUBLIC_ASSISTANT_ENABLED
        value: "true"
      - key: NEXT_PUBLIC_TTS_ENABLED
        value: "true"

databases:
  - name: servio-db
    plan: starter
    region: oregon
    databaseName: servio_production
    user: servio_user
