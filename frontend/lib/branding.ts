/**
 * Canonical branding assets for Servio UI surfaces.
 *
 * Policy:
 * - Use OFFICIAL_SERVIO_LOGO for primary branding (nav, auth pages, headers, footers, SEO schema).
 * - Use SERVIO_DECORATIVE_ICON only for intentionally tiny/decorative contexts where the full logo
 *   would reduce legibility (for example, compact badge chips).
 */
export const OFFICIAL_SERVIO_LOGO = '/images/servio_logo_transparent_tight.png'

export const SERVIO_DECORATIVE_ICON = '/images/servio_icon_tight.png'
